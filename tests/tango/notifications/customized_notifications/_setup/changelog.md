# Changelog — _setup (customized_notifications)

## 2026-06-19 — Created (VCITA2-14248 migration)
- UI login to the isolated account (legacy `Given user logged in to automatic account via API`).
- Seeds `context["nc"] = {"seq": <epoch>}` for unique v3 code_names + titles.
