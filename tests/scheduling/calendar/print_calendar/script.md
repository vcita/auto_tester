# Print Calendar Script

- Preserve UI scheduling before the print action.
- Switch to Month view through Calendar controls.
- Use Playwright download handling and assert the suggested file name contains `Calendar View`.
