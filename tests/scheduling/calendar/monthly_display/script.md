# Monthly Display Script

- Use Calendar view controls and slot cells to create appointments/events through the UI.
- Parse rendered Calendar cards by `data-state`, event type class, title, attendance, and time.
- Assert refresh persistence through the scheduler `view` attribute and date distance.
