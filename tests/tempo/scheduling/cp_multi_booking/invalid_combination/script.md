# CP Multi-Booking — Invalid Service Combination — Detailed Script

> Status: Migrated from automation-js scheduler page objects + verified by focused run.
> Generated By: migration (VCITA2-14228).

## Initial State
- URL: public livesite `{CP_VITRAGE}/site/{pivot_uid}` (anonymous).
- Prerequisites (from _setup): service1/service2, Staff1/Staff2, CP multi booking enabled.
- This test adds service3/service4/event1/service6 + an event1 instance via API first.

## Actions

### Step 0 (API prerequisites)
- `create_service_via_api`: service3 (20m, `client_location`), service4 (60m, Staff2-only),
  event1 (30m, `service_type=event`), service6 (30m). Legacy `user creates new service via API`.
- `schedule_event_via_api(context, event1)`: POST `/v2/event_instances` 20 days out (legacy
  `create_new_event`) so event1 appears in the scheduler future-event list.

### Step 1: Pick "Schedule Now" on the livesite
- Helper: `schedule_now(page, context)` (see summary_component script).

### Step 2: Select service1
- Helper: `toggle_service(page, "service1")` — single click on `span.service-title` (legacy
  `ServiceList.selectService`, no confirm CTA).

### Step 3: Assert disabled set
- Helper: `assert_services_disabled_state({service3: True, service4: True, event1: True})`.
- For each name, locate its `.service-item` row (matched by exact title text), read `class`,
  assert it contains `disabled-service` (legacy `disabledService` selector).

### Step 4: Deselect service1
- Helper: `toggle_service(page, "service1")` (same click toggles off).

### Step 5: Assert all enabled
- Helper: `assert_services_disabled_state({...: False})` for service1, service2, service3,
  service4, service6, event1, In-office appointment, Introductory phone call.
- Each row must NOT contain `disabled-service` (legacy `enabledService`
  `.service-item:not(.disabled-service)`).

### Step 6: Select event1, assert future-event step
- Helper: `toggle_service(page, "event1")` then `assert_next_step_future_event(page)`.
- Wait for `.future-events-list` in the cp_iframe (legacy `CPScheduler.schedulerStepIs("futureEvent")`).

## Success Verification
- service1 disables service3/service4/event1; deselecting re-enables every service; selecting
  event1 advances to the future-event step (real DOM class/element checks, no toasts).
