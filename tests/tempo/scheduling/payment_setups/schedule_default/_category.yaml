name: Schedule Service Default
description: On a fresh account, create six services (require to pay, suggest to pay,
  display a fee, display for a fee, display free, dont display) through the UI, verify
  each renders with the right payment type/price on the services list, schedule an
  appointment for each, and verify the meeting price (100/50/10/blank/Free/blank).
account_profile:
  type: isolated
  slug: ps_schedule_default
  cleanup: always
execution_order:
- schedule_service_default
tests:
- id: schedule_service_default
  name: Schedule Service Default
  status: active
  priority: high
  description: Create the six payment-setting services via UI, verify them on the
    services list, schedule an appointment for each, and verify the meeting prices.
status: active
priority: high
tags:
- scheduling
- services
- payments
- migration
owner: QA Team
created_at: 2026-06-09
stability:
  status: stable
  last_stress_test: '2026-06-15'
  pass_rate: 100.0%
  iterations: 10
