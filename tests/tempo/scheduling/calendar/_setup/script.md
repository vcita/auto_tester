# Calendar Setup Script

- Use runner-provided credentials and auto-account API token.
- Create prerequisites through API because they are setup, not the behavior under test.
- Navigate to Calendar and verify the page is loaded.
