# Three Day Display Script

- Use 3 Days left/middle/right slot mapping from the legacy scheduler helper.
- Preserve the legacy `@wip` scenario instead of reducing scope.
- Parse service color classes when available and verify refresh persistence.
