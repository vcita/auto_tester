# Setup: CRM Filters

Mirrors the legacy `crm-filters-create-and-edit.feature` Background: be logged in
to the back-office of an isolated (fresh) account. Both CRM Filters tests seed
their own clients, products, and custom fields via API/UI, so setup only needs an
authenticated session.

## Steps

1. Log in to the isolated account (username/password from the account profile).
