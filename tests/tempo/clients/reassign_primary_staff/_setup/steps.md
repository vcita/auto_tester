# Setup: Reassign Primary Staff

Mirrors the legacy `reassign-matter-primary-staff.feature` Background: be logged in
to the back-office of an isolated account. The reassignment test seeds its own
client, second staff, service, and appointment via API, so setup only needs an
authenticated session.

## Steps

1. Log in to the isolated account (username/password from the account profile).
