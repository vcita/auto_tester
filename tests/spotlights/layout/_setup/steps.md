# Layout Category Setup

Enable the `new_dashboard` feature flag (so `/app/dashboard` renders the POV new
dashboard) and log in. Mirrors the legacy `icons.feature` Background
(`user logged in to automatic account` + `user navigates to new dashboard`).
