# Set Payment Terms - Detailed Script

> **Status**: Verified with Playwright MCP
> **Last Updated**: 2026-02-11
> **Generated By**: Cursor AI exploration with Playwright MCP

## Initial State
- User is logged in.
- Billing & Invoicing settings are accessible.

## Test Data
```python
import time

timestamp = int(time.time())
payment_terms = f"Test payment terms {timestamp}"
```

## Actions

### Step 1: Navigate to Billing & Invoicing settings
- **Action**: Ensure on Settings, then Billing & Invoicing.
- **Target**: Settings sidebar + Billing & Invoicing card.
- **LOCATOR DECISION**:
| Option | Pros | Cons |
| --- | --- | --- |
| `page.get_by_text("Settings", exact=True)` | Simple | Sidebar may be hidden |
| `settings_scope.get_by_role("button", name="Set your invoices, estimates")` | Direct | Only on Settings hub |
**CHOSEN**: Use Settings sidebar and Billing & Invoicing button when not already on target URL.
**VERIFIED PLAYWRIGHT CODE**:
```python
page.get_by_text("Settings", exact=True).click()
page.wait_for_url("**/app/settings", timeout=45000)
settings_scope.getByRole('button', { name: 'Set your invoices, estimates' }).click()
page.wait_for_url("**/app/settings/billing_and_invoicing", timeout=45000)
```

### Step 2: Open Invoices & Estimates tab
- **Action**: Click "Invoices & Estimates" tab.
- **Target**: Tab "Invoices & Estimates"
- **LOCATOR DECISION**:
| Option | Pros | Cons |
| --- | --- | --- |
| `settings_scope.get_by_role("tab", name=re.compile("^Invoices"))` | Targets tab element | Requires tab role |
| `settings_scope.get_by_text("Invoices & Estimates", exact=True)` | Simple | Could match other text |
**CHOSEN**: `settings_scope.get_by_role("tab", name=re.compile("^Invoices"))`
**VERIFIED PLAYWRIGHT CODE**:
```python
settings_scope.getByRole('tab', { name: /^(Invoices)/ }).click()
```

### Step 3: Set payment due date
- **Action**: Select "Due on receipt".
- **Target**: Radio "Due on receipt"
- **LOCATOR DECISION**:
| Option | Pros | Cons |
| --- | --- | --- |
| `settings_scope.get_by_role("radio", name="Due on receipt")` | Semantic | None |
| `settings_scope.get_by_text("Due on receipt")` | Simple | Not necessarily clickable |
**CHOSEN**: `settings_scope.get_by_role("radio", name="Due on receipt")`
**VERIFIED PLAYWRIGHT CODE**:
```python
settings_scope.getByRole('radio', { name: 'Due on receipt' }).click()
```

### Step 4: Update Terms & conditions
- **Action**: Replace terms text.
- **Target**: Invoice Terms & conditions field (first of invoice + estimate).
- **TWO BILLING UI VARIANTS** (account-dependent rollout; the form renders inside a
  child-app frame that is not always the angularjs iframe, so resolve the frame by the
  unique "Due on receipt" radio, then work inside it):
  - **Vue UI**: accessible textbox -> `frame.get_by_role("textbox", name=/Terms & conditions/).first`
  - **Legacy Angular UI**: md-input textareas with no accessible name; invoice terms is
    the first `textarea.explicit-overflow` (estimate terms is second).
- **CHOSEN**: wait for either selector to become visible inside the form frame; reload the
  page between attempts to re-mount a stalled form.
- **Note**: Prefer `fill()` first; fallback to sequential typing only if value does not stick.
- **TODO(app)**: add a stable `data-qa` to the invoice terms field to drop the CSS fallback.

### Step 5: Save settings
- **Action**: Click Save.
- **Target**: Save button in Billing & Invoicing settings.
- **VERIFIED PLAYWRIGHT CODE**:
```python
settings_scope.getByRole('button', { name: 'Save' }).click()
```

## Success Verification
- "Due on receipt" radio is selected.
- Terms & conditions textbox value equals `payment_terms`.
