# Set Receipt Settings - Detailed Script

> **Status**: Verified with Playwright MCP (receipt settings unavailable without gateway)
> **Last Updated**: 2026-02-12
> **Generated By**: Cursor AI exploration with Playwright MCP

## Initial State
- User is logged in.
- Payment gateway is NOT connected.

## Actions

### Step 1: Open Payments settings
- **Action**: Navigate to Settings and open Payments.
- **Target**: Settings sidebar + Payments card.
**VERIFIED PLAYWRIGHT CODE**:
```python
page.get_by_text("Settings", exact=True).click()
payments_button = settings_scope.get_by_role("button", name="Connect your preferred payment provider")
if payments_button.count() == 0:
    # Without gateway this section may be unavailable in current account view
    mark_unavailable_and_exit()
payments_button.first.click()
```

### Step 2: Open Terms & Policies
- **Action**: Click "Terms & Policies" tab.
- **Target**: Payments settings tab.
**VERIFIED PLAYWRIGHT CODE**:
```python
terms_tab = settings_scope.get_by_text("Terms & Policies", exact=True)
if terms_tab.count() == 0:
    mark_unavailable_and_exit()
terms_tab.first.click()
```

### Step 3: Detect receipt settings
- **Action**: Look for any receipt-related controls. If enabled, toggle and save.
- **Target**: Receipt checkboxes/fields (if available).

## Success Verification
- Receipt settings are updated if available.
- Otherwise store status as unavailable without gateway.

