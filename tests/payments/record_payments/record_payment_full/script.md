# Record Full Payment - Detailed Script

> **Status**: Verified with Playwright MCP
> **Last Updated**: 2026-03-04
> **Generated By**: Cursor AI exploration with Playwright MCP

## Initial State
- User is logged in (from payments _setup)
- On dashboard or any app page
- No payment gateway connected

## Actions

### Step 1: Navigate to Checkout

- **Action**: Click "Sales" in sidebar to open Checkout page
- **Target**: Sales button in left sidebar

**LOCATOR DECISION:**

| Option | Pros | Cons |
|--------|------|------|
| `locator("button").filter(has_text="Sales")` | Matches sidebar button directly, verified in MCP | Might match other buttons with "Sales" text |
| `locator('[data-qa="nav-sales"]')` | data-qa is most stable | Not visible in accessibility tree, needs verification |
| `get_by_role("button", name="Sales")` | Semantic, accessible | May match multiple buttons |

**CHOSEN**: `locator("button").filter(has_text="Sales")` - Verified in MCP, directly navigates to /app/pos.

**VERIFIED PLAYWRIGHT CODE**:
```python
sales_button = page.locator("button").filter(has_text="Sales")
sales_button.click()
page.wait_for_url("**/app/pos**", timeout=30000, wait_until="domcontentloaded")
```

- **How verified**: Clicked in MCP, page navigated to https://app.vcita.com/app/pos (Checkout page)
- **Wait for**: URL contains "/app/pos"
- **Fallback locators**: `[data-qa="nav-sales"]`, `get_by_role("button", name="Sales")`

### Step 2: Add a Custom Item with a cost

- **Action**: Click "Custom Item" button to open the custom item dialog
- **Action**: Fill Name and Price fields
- **Action**: Click "Add" to add the item to cart
- **Target**: Custom Item button in the tab bar

**LOCATOR DECISION:**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("button", name="Custom Item")` | Semantic, unique button name | None |
| `locator("text=Custom Item")` | Text-based | Less specific |

**CHOSEN**: `get_by_role("button", name="Custom Item")` - Unique and semantic.

**VERIFIED PLAYWRIGHT CODE**:
```python
import time as _time
item_name = f"Test Payment {int(_time.time())}"
item_price = "50"

custom_item_button = page.get_by_role("button", name="Custom Item")
custom_item_button.wait_for(state="visible", timeout=30000)
custom_item_button.click()

name_field = page.get_by_role("textbox", name="Name*")
name_field.wait_for(state="visible", timeout=30000)
name_field.click()
page.wait_for_timeout(100)
name_field.press_sequentially(item_name)

price_field = page.get_by_role("spinbutton", name="Price*")
price_field.click()
price_field.press_sequentially(item_price)

add_button = page.get_by_role("button", name="Add")
add_button.wait_for(state="visible", timeout=30000)
add_button.click()
```

- **How verified**: Filled name "Test Payment Item" and price "50", clicked Add. Custom item appeared in cart with ₪50.00
- **Wait for**: Custom item appears in cart panel, dialog closes
- **Fallback locators**: `locator("text=Custom Item")` for button, `get_by_label("Name*")` for name field

### Step 3: Select a client

- **Action**: Click "Select Property" button to open client selector
- **Action**: Select the first client from the "Recently Active" section
- **Target**: Select Property button in checkout panel, then client in iframe dialog

**LOCATOR DECISION (Select Property button):**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("button", name="Select Property")` | Semantic, unique | None |
| `locator("text=Select Property")` | Text-based | Less specific |

**CHOSEN**: `get_by_role("button", name="Select Property")` - Unique and semantic.

**LOCATOR DECISION (Client in iframe dialog):**

| Option | Pros | Cons |
|--------|------|------|
| `dialog.get_by_text(client_name, exact=False).first` | Targets the setup client by name | Can match a non-selectable text node, dismissing the picker without selecting (regression that left Checkout disabled) |
| `dialog.get_by_role("listitem").filter(has_text=client_name).first` | Clicks the actual client row | Requires the row to be visible (filter search first) |
| `dialog.get_by_role("list").nth(1).get_by_role("listitem").first` | Positional in recently active list | Fragile if list order changes |

**CHOSEN**: Click an actual client **row** (`md-list-item[role=listitem]`), then **verify the Checkout button becomes enabled**. The picker opens on "Recently Active"; when the desired client is not there (e.g. a freshly created client that is not yet recently active), switch to the **"ALL CLIENTS"** view and **search** by name. Never click the first row, which is the "New Client" action. Retry up to 3 times; if Checkout never enables, fail with an explicit message instead of a blind 30s click timeout.

**Picker structure (verified via CDP on the angularjs iframe)**:
- Rows are Angular Material `md-list-item[role=listitem]`; `get_by_role("list")` does NOT match (no `role=list`).
- First row = "New Client" action (no email). Real clients contain an email.
- "Recently Active" can be empty for a brand-new client; "ALL CLIENTS" + search surfaces it (search box appears after switching).

**VERIFIED PLAYWRIGHT CODE**:
```python
SELECT_CLIENT_PATTERN = re.compile(r"^Select (Client|Property)$", re.I)
EMAIL_PATTERN = re.compile(r"@")

def _open_client_picker(page):
    page.get_by_role("button", name=SELECT_CLIENT_PATTERN).click()
    dialog = page.frame_locator('iframe[title="angularjs"]').get_by_role("dialog")
    dialog.wait_for(state="visible", timeout=30000)
    return dialog

def _pick_client_row(dialog, client_name):
    rows = dialog.get_by_role("listitem")
    if client_name:
        named = rows.filter(has_text=client_name).first
        if named.count() > 0:
            return named
    real = rows.filter(has_text=EMAIL_PATTERN).first   # real clients have an email; skip "New Client"
    return real if real.count() > 0 else None

client_name = context.get("created_client_name") or context.get("invoice_client_search_term")
checkout_button = page.get_by_role("button", name="Checkout")
for _ in range(3):
    dialog = _open_client_picker(page)
    target = _pick_client_row(dialog, client_name)
    if target is None:                                  # not recently active -> All Clients + search
        dialog.get_by_text(re.compile(r"^\s*all clients\s*$", re.I)).first.click()
        if client_name:
            search = dialog.get_by_role("textbox").first
            if search.count():
                search.fill(client_name); page.wait_for_timeout(1200)
        target = _pick_client_row(dialog, client_name)
    if target is not None:
        target.click()
        try:
            expect(checkout_button).to_be_enabled(timeout=8000); break
        except AssertionError:
            pass
    if dialog.is_visible():
        page.keyboard.press("Escape")
```

- **Root cause**: the previous `get_by_text(client_name).first` clicked a non-selectable text node that closed the picker without choosing a client, leaving Checkout disabled so Step 4's click timed out (30s).
- **Wait for**: Checkout button becomes enabled (the reliable signal that a client + item are selected).
- **Fallback**: "ALL CLIENTS" view + name search, then the first real client row.

### Step 4: Click Checkout

- **Action**: Click the "Checkout" button
- **Target**: Checkout button at the bottom of the cart panel

**LOCATOR DECISION:**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("button", name="Checkout")` | Semantic, unique after client selected | Disabled until client + item selected |

**CHOSEN**: `get_by_role("button", name="Checkout")` - Unique and semantic.

**VERIFIED PLAYWRIGHT CODE**:
```python
checkout_button = page.get_by_role("button", name="Checkout")
checkout_button.wait_for(state="visible", timeout=30000)
checkout_button.click()
```

- **How verified**: Clicked in MCP, payment options menu appeared
- **Wait for**: Menu with payment options appears
- **Fallback locators**: `locator("button:has-text('Checkout')")`

### Step 5: Select "Record payment"

- **Action**: Click "Record payment" from the payment options menu
- **Target**: Record payment option in the menu

**LOCATOR DECISION:**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("menu").get_by_text("Record payment")` | Scoped to menu, readable | Menu role must be present |
| `locator("div").filter(has_text="Record payment").nth(5)` | Raw MCP output | Fragile index |
| `get_by_text("Record payment")` | Simple | May match elements elsewhere |

**CHOSEN**: `get_by_role("menu").get_by_text("Record payment")` - Scoped to the menu for uniqueness.

**VERIFIED PLAYWRIGHT CODE**:
```python
menu = page.get_by_role("menu")
menu.wait_for(state="visible", timeout=30000)
record_payment_option = menu.get_by_text("Record payment")
record_payment_option.click()
```

- **How verified**: MCP clicked the "Record payment" option from the payment menu, Record Payment dialog opened in iframe
- **Wait for**: Record Payment dialog appears in iframe
- **Fallback locators**: `get_by_text("Record payment")`, `locator("div").filter(has_text="Record payment")`

### Step 6: Select "Cash" payment method and click Record

- **Action**: Click "Payment received via" listbox and select "Cash"
- **Action**: Click "Record" button
- **Target**: Payment method listbox and Record button in iframe dialog

**LOCATOR DECISION (Listbox):**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("listbox", name="Payment received via")` | Semantic, unique | None |

**CHOSEN**: `get_by_role("listbox", name="Payment received via")` - Unique and semantic.

**LOCATOR DECISION (Cash option):**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("option", name="Cash")` | Semantic, unique | None |

**CHOSEN**: `get_by_role("option", name="Cash")` - Unique and semantic.

**LOCATOR DECISION (Record button):**

| Option | Pros | Cons |
|--------|------|------|
| `get_by_role("button", name="Record")` | Semantic, unique in dialog | None |

**CHOSEN**: `get_by_role("button", name="Record")` - Unique in the dialog.

**VERIFIED PLAYWRIGHT CODE**:
```python
iframe = page.frame_locator('iframe[title="angularjs"]')

dialog = iframe.get_by_role("dialog")
dialog.wait_for(state="visible", timeout=30000)

method_listbox = iframe.get_by_role("listbox", name="Payment received via")
method_listbox.wait_for(state="visible", timeout=30000)
method_listbox.click()

cash_option = iframe.get_by_role("option", name="Cash")
cash_option.click()

record_button = iframe.get_by_role("button", name="Record")
record_button.wait_for(state="visible", timeout=30000)
record_button.click()
```

- **How verified**: Selected Cash from listbox, clicked Record. Dialog closed and page redirected to payment details page.
- **Wait for**: Dialog closes, page navigates to /app/payments/transactions/{id}
- **Fallback locators**: `iframe.locator("text=Cash")` for option, `iframe.locator("button:has-text('Record')")` for button

## Success Verification

- **Action**: Verify redirect to payment transaction page and check payment details
- **Expected**: URL contains `/app/payments/transactions/`, "Paid" status visible, amount matches

**VERIFIED PLAYWRIGHT CODE**:
```python
page.wait_for_url("**/app/payments/transactions/**", timeout=30000, wait_until="domcontentloaded")

iframe = page.frame_locator('iframe[title="angularjs"]')

paid_status = iframe.get_by_text("Paid", exact=True).first
paid_status.wait_for(state="visible", timeout=30000)

cash_indicator = iframe.get_by_text("Cash", exact=True)
cash_indicator.wait_for(state="visible", timeout=30000)

payment_id = page.url.split("/")[-1]
context["recorded_payment_id"] = payment_id
context["recorded_payment_amount"] = item_price
context["recorded_payment_method"] = "Cash"
```

- **How verified**: After recording payment, page redirected to `/app/payments/transactions/6dim0bch0uautekj`. Page shows "Paid" status, "Cash" method, "₪50.00" amount, and client name.
- **Save to context**: recorded_payment_id, recorded_payment_amount, recorded_payment_method
