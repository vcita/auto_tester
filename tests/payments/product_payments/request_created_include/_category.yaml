# Product payment request created - tax mode include (isolated account)
# Migrated from products.feature scenario 3 "payments request created for product in mode include" (VCITA2-13858).

name: Product Request Created Include
description: On a fresh account (point_of_sale denied, tax mode "include") assign a $10 product with two taxes (13% + 13.13%) to "first last" via the client card, and assert the product payment request is DUE $10.00 (tax included in price).

account_profile:
  type: isolated
  slug: prodpay_incl
  cleanup: always

tests:
  - id: request_include
    name: Product payment request created (tax include)
    status: active
    priority: high
    description: Assign payable_item1 with two taxes via the client card and assert DUE $10.00 (inclusive mode).

status: active
priority: high
tags:
  - payments
  - products
  - migration
owner: QA Team
created_at: 2026-06-06
