# Edit Invoice - Detailed Script

> **Status**: Verified with Playwright MCP
> **Last Updated**: 2026-02-12
> **Generated By**: Cursor AI exploration with Playwright MCP

## Initial State
- User is logged in.
- Payment gateway is NOT connected.

## Actions

### Step 1: Open Billing & Invoicing
- **Action**: Navigate via Sales sidebar -> Billing & Invoicing.
**VERIFIED PLAYWRIGHT CODE**:
```python
page.get_by_role("button", name="Sales").click()
page.get_by_text("Billing & Invoicing", exact=True).click()
```

### Step 2: Open invoice and edit
- **Action**: Open first invoice and use actions menu -> Edit.
**VERIFIED PLAYWRIGHT CODE**:
```python
invoice_scope.locator("md-menu").filter(has_text="Edit").get_by_role("button").click()
invoice_scope.get_by_role("menuitem", name="Edit").click()
```

### Step 3: Add another line item
- **Action**: Use item selector to add another service.
**VERIFIED PLAYWRIGHT CODE**:
```python
editor_scope.get_by_role("textbox", name="Please select an item").click()
editor_scope.get_by_role("option", name=re.compile("Event Test Workshop")).first.click()
```

### Step 4: Save changes
- **Action**: Click Save draft/Save and verify total changes.

## Success Verification
- Total amount changes after edit.
- Update `created_invoice_amount` in context.

