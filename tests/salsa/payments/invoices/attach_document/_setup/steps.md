# Setup: Attach Document to Invoice

Mirrors the legacy `attach-document-to-invoice.feature` Background: be logged in to
the documents/back-office of an isolated account. The client and the "display a fee"
service are created per-test via API (see the test's `script.md`).

## Steps

1. Log in to the isolated account (username/password from the account profile).
