# Event Payments (grouping subcategory)
# Migrated from automation-js/features/salsa/event-payments.feature (VCITA2-13856)
# Each legacy scenario runs on a fresh automatic account, and the point_of_sale
# feature flag conflicts across scenarios (some deny it, the POS scenario needs
# it), so every scenario is its own isolated child subcategory with its own
# account + Background (event service + scheduled event + registered client).

name: Event Payments
description: Event payment-request flows migrated from event-payments.feature - request lifecycle (create/edit/cancel), paying (partial + full), POS record-payment, cancel & refund, invoiced event, and package redemption. Each scenario uses its own isolated account.

execution_order:
  - payment_request_lifecycle
  - pay_for_event
  - pay_via_pos
  - cancel_refund
  - invoiced_event
  - redeem_package

status: active
priority: high
tags:
  - payments
  - events
  - migration
owner: QA Team
created_at: 2026-06-06
