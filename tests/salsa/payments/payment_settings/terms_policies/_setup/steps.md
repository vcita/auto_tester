# Terms and Policies — Setup

## What it does
1. Log in to the isolated account (UI).
2. Connect the mock payment gateway (providers dialog) — the legacy precondition.

## Notes
- The terms & policies scenario runs with a payment provider connected, mirroring the
  legacy `configure mock payment gateway` precondition.
