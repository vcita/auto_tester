# Changelog — generate_pdfs setup

## 2026-06-07 — Initial migration (VCITA2-13902)
- Mirrors the legacy generate_pdfs.feature Background (API-only): creates the shared
  client `first last` via API. No UI login — all PDF scenarios are API-driven.
