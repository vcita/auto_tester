# Setup: External receipt - POS

Prerequisites for the POS external-receipt scenario (not the behaviour under test).

1. Log in to the isolated account (point_of_sale stays enabled so the POS is available).
2. Create the `simon bolivar` client via API.
3. Assign the `mockreceipts` external-receipt app to the business via API.
